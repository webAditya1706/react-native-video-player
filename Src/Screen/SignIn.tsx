import React from 'react'
import { SafeAreaView, ScrollView, Text } from 'react-native'

const SignIn = () => {
  return (
	<SafeAreaView>
		<ScrollView>
			<Text>SignIn</Text>
		</ScrollView>
	</SafeAreaView>
  )
}

export default SignIn
