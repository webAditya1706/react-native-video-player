import React from 'react'
import { SafeAreaView, ScrollView, Text } from 'react-native'

const SignUp = () => {
  return (
	<SafeAreaView>
		<ScrollView>
			<Text>SignIn</Text>
		</ScrollView>
	</SafeAreaView>
  )
}

export default SignUp
